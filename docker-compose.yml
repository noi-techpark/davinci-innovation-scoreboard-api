version: "3"
services:
  java:
    build:
      context: .
      dockerfile: docker/dockerfile-java
    volumes:
       - ./:/code
    working_dir: /code
    ports:
      - "8080:8080"
    command: mvn spring-boot:run
    tty: true
